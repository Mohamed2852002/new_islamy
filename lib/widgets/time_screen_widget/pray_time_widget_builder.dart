import 'package:flutter/material.dart';

class PrayTimeWidgetBuilder extends StatelessWidget {
  const PrayTimeWidgetBuilder({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}